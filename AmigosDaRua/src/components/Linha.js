import React from 'react';
import { View } from 'react-native';

export const Linha = () => {
  return (
    <View
      style={{
        height: 1,
        backgroundColor: '#C8C8C8',
      }}
    />
  );
};

export default Linha;